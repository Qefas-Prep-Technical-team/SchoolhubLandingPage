import React, { FC } from 'react';

const page: FC = () => {
    return (
        <div className="flex h-full w-full items-center justify-center">
            <h1> features</h1>
        </div>
    );
};

export default page;